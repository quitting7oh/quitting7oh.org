---
import type { PaginationNeighbor } from '~/lib/pagination';

interface Props {
  prev?: PaginationNeighbor;
  next?: PaginationNeighbor;
}

const { prev, next } = Astro.props;
---

{(prev || next) && (
  <nav
    aria-label="Page navigation"
    class="mt-14 grid gap-3 sm:grid-cols-2"
  >
    {prev ? (
      <a
        href={prev.href}
        rel="prev"
        class="field-card group block p-5 transition-colors hover:bg-accent sm:p-6"
      >
        <div class="eyebrow !text-muted-foreground">
          ← Previous
        </div>
        <div class="mt-2 font-display text-lg font-semibold leading-tight text-foreground group-hover:text-primary">
          {prev.title}
        </div>
      </a>
    ) : (
      <div class="hidden sm:block" aria-hidden="true"></div>
    )}
    {next && (
      <a
        href={next.href}
        rel="next"
        class="field-card group block p-5 transition-colors hover:bg-accent sm:p-6 sm:text-right"
      >
        <div class="eyebrow !text-muted-foreground">
          Next →
        </div>
        <div class="mt-2 font-display text-lg font-semibold leading-tight text-foreground group-hover:text-primary">
          {next.title}
        </div>
      </a>
    )}
  </nav>
)}
